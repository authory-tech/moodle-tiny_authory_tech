docker compose up -d
open http://localhost:8080    # Moodle  (admin / Admin123!)
# open http://localhost:8090    # Swagger UI — Moodle web services
# open http://localhost:8091    # Swagger UI — ML server
# Real ML server (type-server) listening at http://localhost:8084
# From inside the Moodle container it is reachable at http://type-server-app-1:8084

